# multi-region-profile-service
Production-grade Spring Boot WebFlux service for globally low-latency user profile reads using primary-region PostgreSQL writes, Kafka profile change replication, regional PostgreSQL read models, Redis caching, transactional outbox, consistency SLA monitoring, and operational replay tooling.
